"
I keep track on how to render a component and also rendering of children.

**Vars**

children - subcomponents
name - give component a name
id - id for the html tag. Will often be autogenerated.
h - renderer object
extraAttributes - add more attributes here if you not want to change render method
page - nil or top page, each component has a ref to the top page
isPage
"
Class {
	#name : 'LVComponent',
	#superclass : 'LVHtml',
	#instVars : [
		'name'
	],
	#category : 'LightView-rendering',
	#package : 'LightView',
	#tag : 'rendering'
}

{ #category : 'initialization' }
LVComponent >> initialize [

	super initialize.
	children := OrderedCollection new.
	extraAttributes := {  }
]

{ #category : 'accessing' }
LVComponent >> name [

	^ name
]

{ #category : 'accessing' }
LVComponent >> name: anObject [

	name := anObject
]

{ #category : 'rendering' }
LVComponent >> render [
	"A component MUST have a unique name. Overwrite this method if you need other attributes or another base tag"

	h
		div: (({
				(#class -> 'lv-component').
				(#name -> name) }) , extraAttributes)
		with: [ self renderChildren ]
]

{ #category : 'accessing' }
LVComponent >> renderer [

	^ h
]
