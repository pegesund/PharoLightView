"
I keep track on how to render a component and also rendering of children.

**Vars**

children - subcomponents
name - give component a name
id - id for the html tag. Will often be autogenerated.
h - renderer object
extraAttributes - add more attributes here if you not want to change render method
page - nil or top page, each component has a ref to the top page
isPage
"
Class {
	#name : 'LVComponent',
	#superclass : 'LVHtml',
	#category : 'LightView-components',
	#package : 'LightView',
	#tag : 'components'
}

{ #category : 'instance creation' }
LVComponent class >> new [

	self error: 'Use newName:id: instead of new'
]

{ #category : 'instance creation' }
LVComponent class >> newName: n id: id [

	| c |
	c := super new.
	super initialize.
	c name: n.
	c id: id.
	c extraAttributes: { ('id' -> id) . ( 'name' -> n) }.
	^ c
]

{ #category : 'instance creation' }
LVComponent class >> superNew [
	| c |
	c := super new.
	super initialize.
   self initialize .
   ^ c.
]

{ #category : 'rendering' }
LVComponent >> html [
	"A component MUST have a unique name. Overwrite this method if you need other attributes or another base tag"

	h
		div: {
				(#class -> 'lv-component').
				(#name -> name) } 
		with: [ self renderChildren ]
]

{ #category : 'accessing' }
LVComponent >> request: req [
	"Overwrite this to answer a request"
	^ self render.

	
]
